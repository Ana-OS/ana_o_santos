const info = window.addEventListener("load", () => {
  const arrow = document.querySelector("#arrow").addEventListener("click", () => {const el = document.querySelector("#app").scrollIntoView({ behavior: "smooth"});
    });
})

export {info}
